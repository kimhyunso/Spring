spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/batch_repo?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: batch_user
    password: batch

  sql:
    init:
      mode: never
      schema-locations: classpath:schema-mysql.sql

